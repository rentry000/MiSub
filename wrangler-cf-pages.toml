# 🔥 唯一正确：纯 Cloudflare Workers 静态网站部署配置
name = "misub"
compatibility_date = "2024-09-23"

# 核心：托管 ./dist 静态文件（你的前端网站）
assets = { directory = "./dist" }

# 单页应用(Vue/React)必备：刷新不404
[assets]
directory = "./dist"
not_found_handling = "rewrite_to_index"
